import math
"""def solution(area: int):
    area_list = list()
    for row in reversed(range(1, area + 1)):
        if row ** 2 <= (area - row):
            area_list.append(row)

    return print(area_list)"""
"""def solution(area: int):
    area_list = list()
    while area > 0:




y = math.sqrt(12)
z = 12 % 2
print(y, z)

if __name__ == '__main__':

    print(solution(12))"""

"""if not area_list:
for row in reversed(range(area + 1)):
    if area % row == 0:
        area_list.append(row)
    if (area - area_list[-1]) % row == 0:
        area_list.append(row)
"""
"""elif area_list:        
    for row in reversed(range(1, (area - area_list[-1]) + 1)):
        if area_list[-1] % row == 0:
            area_list.append(row)"""



